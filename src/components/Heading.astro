---
interface Props {
  title: string;
  subtitle: string;
}
const { title, subtitle } = Astro.props;
---

<section class="text-center">
  <h1 class="font-bold text-5xl text-white mb-9">{title}</h1>
  <p class="text-white text-xl">{subtitle}</p>
</section>
